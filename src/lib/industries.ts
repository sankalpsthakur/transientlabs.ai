/** Primary proof-strip industries shown on the homepage. */
export const INDUSTRY_NAMES = [
  'Biochar',
  'Submersible pumps',
  'Industrial teams',
  'Hardware programs',
] as const;

/** Demoted from the primary strip — still shipped, not catalog focus. */
export const ALSO_SHIPPED_INDUSTRIES = [
  'Retail',
  'AI-native SaaS',
  'Finance close / shared services',
] as const;
